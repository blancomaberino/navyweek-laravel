includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: max
    paths:
        - app
    # The domain boundary: callers depend on repository interfaces, never on
    # Eloquent directly. Tighten as the codebase grows.
    checkModelProperties: true
    checkOctaneCompatibility: false
    ignoreErrors: []
